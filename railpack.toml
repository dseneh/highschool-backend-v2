# Railpack configuration for ezyschool backend
# Provides PostgreSQL client tools (pg_dump, pg_restore) for backup/restore workers

[nixpkgs]
# Include PostgreSQL client tools in the runtime image
packages = ["postgresql"]

