[phases.setup]
# Tenant backup and restore workers shell out to PostgreSQL's native archive
# tools. The ellipsis preserves Nixpacks' automatically detected packages.
nixPkgs = ["...", "postgresql_16"]
